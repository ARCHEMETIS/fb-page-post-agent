# แผนคอนเทนต์เพจ (content plan)

ไฟล์นี้คือ **ลำดับการเล่าเรื่อง** ของเพจ agent ต้องอ่านไฟล์นี้ก่อนเลือกหัวข้อทุกครั้ง
แก้ไฟล์นี้ได้ตลอด — เปลี่ยนลำดับ เพิ่ม/ตัดหัวข้อ หรือย้ายสถานะ ได้ตามใจ

## กติกา

1. **เล่าตามลำดับ** หยิบหัวข้อที่ยังเป็น `[ ]` ที่อยู่บนสุด อย่าข้ามไปหยิบอันล่างถ้าอันบนยังไม่ได้ลง
   เว้นแต่มีเหตุผลชัดเจน (เช่น มีข่าวใหญ่ที่ต้องเกาะกระแสวันนั้น) — ถ้าข้าม ให้บอกเหตุผลในรายงาน
2. **หัวข้อที่ลงแล้วให้เปลี่ยนเป็น `[x]`** พร้อมใส่ชื่อไฟล์ draft ต่อท้าย แล้ว commit มาพร้อมกับ draft
3. ถ้าหัวข้อที่ถึงคิวต้องใช้ข้อมูลส่วนตัวที่ agent ไม่รู้ ให้เว้น `[[ ใส่ตรงนี้: ... ]]` ตามปกติ **อย่าเปลี่ยนไปหยิบหัวข้ออื่นเพราะเขียนอันนี้ยาก**
4. ถ้าหัวข้อใน `[ ]` หมดแล้ว ค่อยเลือกหัวข้อใหม่เองได้ตามสัดส่วนที่กำหนดใน prompt แล้วเติมกลับเข้ามาในไฟล์นี้

## ลิงก์ที่ใช้

- โพสต์ที่เขียนให้ **ผู้เรียน** (ค่าเริ่มต้น) → `https://luiquest.netlify.app`
- โพสต์ที่เขียนให้ **นักพัฒนา** เท่านั้น (ระบุไว้ท้ายหัวข้อ) → ลิงก์ GitHub ได้

---

## ลำดับ

- [x] **1. เปิดตัว: ลุยเควสคืออะไร และเริ่มมาจากไหน** — นิสัยขอ roadmap จาก AI แล้วไม่ได้ทำ → ให้ส่งเควสรายวันแทน → เขียน ml-quest ใช้เองคนเดียว → วิชาพัฒนานวัตกรรม เพื่อนเห็นด้วย อาจารย์ผ่าน → ขยายเป็นลุยเควส เรียนอะไรก็ได้ → เพื่อนทำ UI เขาทำแบ็กเอนด์ → ปิดท้ายว่ายังมีผู้ใช้คนเดียวคือตัวเอง *(เขียนมือ ลงแล้ว)*

- [x] **2. ใช้ลุยเควสยังไง — แนะนำการใช้งานล้วนๆ** — `drafts/pending/2026-08-09-how-to-use-luiquest.json`

  **ประเภท: คู่มือการใช้งาน ไม่ใช่เรื่องเล่า** โพสต์นี้ยกเว้นกฎ "เปิดด้วยฉาก" ใน prompt
  เพราะคนอ่านมาหาคำตอบว่าใช้ยังไง ไม่ได้มาอ่านเรื่องราว
  เขียนตรงไปตรงมา เป็นลำดับขั้น อ่านแล้วทำตามได้ทันที
  แต่ยังต้องเป็นภาษาคน ไม่ใช่คู่มือราชการ และห้ามมีกลิ่น AI ตามกฎเดิม
  ไม่ต้องมีคำถามชวนเล่าเรื่องท้ายโพสต์ — ปิดด้วยชวนไปลองใช้พอ

  **ฟังก์ชันที่ยืนยันแล้ว ใช้ได้เลย ไม่ต้องเดา:**
  - เข้าที่ luiquest.netlify.app แล้วล็อกอิน
  - เลือกหัวข้อที่อยากเรียน — มีชุดที่คัดมาให้ (เช่น Data/ML) **และพิมพ์หัวข้ออิสระเองได้แล้ว**
  - ระบบตัด roadmap ออกเป็นเควสรายวัน แล้วแสดงทีละวัน ไม่เห็นสารบัญทั้งก้อน
  - หน้าเควสวันนี้บอก: ชื่อเควส, เวลาที่ใช้ (เช่น 60 นาที), XP ที่ได้ (เช่น 10 XP),
    และหัวข้อ "จบวันนี้จะทำอะไรได้" เป็นข้อๆ
  - มีเช็คลิสต์ให้ติ๊ก (เช่น 4 ข้อ) ติ๊กครบได้ XP
  - แต่ละข้อมีลิงก์แหล่งเรียนที่คัดมาให้แล้ว — ของจริงที่เห็นคือ Kaggle Learn,
    คลิปช่อง PrasertCBS, บทความ DataRockie ไม่ต้องไปหาเอง
  - แถบสถิติด้านบน: XP, streak, ความคืบหน้า roadmap เป็น %, อันดับ
  - เลเวลเป็นเกรด F → D → C → B → A พร้อมแถบ XP (เช่น 10/25 XP ไป Lv.2)
  - ปุ่ม "ถามโค้ช" ลอยอยู่มุมขวาล่าง กดถามได้ตอนติด
  - แถบล่างมี 3 เมนู: เควสวันนี้ / แชทโค้ช / อันดับ
  - ติดตั้งลงหน้าจอโฮมมือถือได้ (PWA)
  - ถ้า streak ขาด ระบบไม่ด่า ขึ้นข้อความให้กำลังใจแล้วเริ่มนับใหม่

  **เรื่องราคา — เขียนตามนี้เป๊ะ อย่าเกินกว่านี้:**
  มี subscription ที่เรียนได้ไม่จำกัดหัวข้อ **แต่โพสต์นี้ไม่ใช่โพสต์ขายของ**
  เจ้าของอยากให้คนได้ลองของฟรีก่อน ไม่ใช่เจอกำแพงราคาตั้งแต่ยังไม่รู้ว่าแอปเป็นยังไง
  พูดถึงได้แค่สั้นๆ ระดับ "ลองใช้ฟรีได้เลย ถ้าติดใจแล้วอยากเรียนหลายหัวข้อค่อยว่ากัน"
  ห้ามใส่ราคา ห้ามเร่งให้สมัคร ห้ามทำเป็นหัวข้อหลักของโพสต์

- [x] **3. "เสร็จแล้ว" ไม่เท่ากับ "เข้าใจแล้ว"** — ถามก่อนเรียนและหลังเรียน → `drafts/submitted/2026-08-08-completion-is-not-understanding.json` *(เขียนแล้ว รอลง — อย่าเขียนซ้ำ)*

- [ ] **4. ออกแบบให้คน "พลาดได้"** — เบื้องหลังข้อความตอน streak ขาด ("คนเก่งไม่ใช่คนไม่เคยพลาด แต่คือคนที่กลับมาลุยต่อ") ทำไมถึงไม่ใช้ความรู้สึกผิดแบบ Duolingo *(รอคำตอบ — ดู `needs-input/2026-08-10-design-for-missing-a-day.md`)*
- [ ] **5. ทำไมเลเวลเป็น F D C B A** — ไม่ใช่ตัวเลข *(รอคำตอบ — ดู `needs-input/2026-08-11-why-levels-are-letter-grades.md`)*
- [ ] **6. ยิ่งคนใช้เยอะ AI ยิ่งพัง** — โควตา LLM free tier นับต่อโปรเจกต์ ไม่ใช่ต่อผู้ใช้ *(โพสต์สายเดฟ — รอคำตอบ ดู `needs-input/2026-08-12-ai-quota-per-project.md`)*
- [x] **7. กติกา 0 บาท** — สถาปัตยกรรมของคนไม่มีงบ *(โพสต์สายเดฟ)* — `drafts/pending/2026-08-13-zero-baht-architecture.json`
- [ ] **8. ทำไมแอปเรียนถึงต้องมีผี** — มาสคอตกับการกลับมาใช้ซ้ำ *(รอคำตอบ — ดู `needs-input/2026-08-14-why-a-ghost-mascot.md`)*
- [ ] **9. ชวนมาเป็นผู้ทดลองใช้** — ขอฟีดแบ็กจริงจัง *(รอคำตอบ — ดู `needs-input/2026-08-15-invite-testers-and-feedback.md`, พบ commit ที่ขัดกับ author-notes.md เรื่องจำนวนผู้ใช้)*

## หัวข้อสำรอง (ยังไม่จัดลำดับ)

- ผม vibe code แอปนี้ทั้งตัว — ข้อดี ข้อเสีย และสิ่งที่ยังต้องเข้าใจเอง
- data leakage ตอนตัดเฟรมจากวิดีโอ → `drafts/submitted/2026-08-08-copper-dome-turret.json` *(เขียนแล้ว ยังไม่ลง)*
- ทำไมถึงเลือก visual servoing แทนการคำนวณพิกัดโลกจริง (copper-dome-turret)
- Google Sheets เป็น CMS แทนฐานข้อมูล (muslim-club-website)
- requests + BeautifulSoup เร็วกว่า Selenium สิบเท่า (news-pdf-keyword-scraper)
